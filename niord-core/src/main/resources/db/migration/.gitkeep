# Flyway migrations live here, beside the entities they serve, and ship in the
# niord-core jar. The first one was generated from the entity model; the ones
# after it are hand-written, because an incremental change to a live schema is a
# decision (guarded or not, backfilled or not) rather than a diff.
